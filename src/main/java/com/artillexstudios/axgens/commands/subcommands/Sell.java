package com.artillexstudios.axgens.commands.subcommands;

import com.artillexstudios.axgens.enums.SellType;
import org.bukkit.entity.Player;
import org.jetbrains.annotations.NotNull;

public enum Sell {
    INSTANCE;

    public void execute(@NotNull Player sender, @NotNull SellType type) {
    }
}
