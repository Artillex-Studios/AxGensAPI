package com.artillexstudios.axgens.commands.subcommands;

import org.bukkit.command.CommandSender;
import org.bukkit.entity.Player;
import org.jetbrains.annotations.NotNull;
import org.jetbrains.annotations.Nullable;

public enum Sellwand {
    INSTANCE;

    public void execute(@NotNull CommandSender sender, Player player, @NotNull com.artillexstudios.axgens.sellwand.Sellwand sellwand, @Nullable Integer amount) {
    }
}
